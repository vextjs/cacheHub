# cache-hub v2.2.2

> Release date: 2026-06-09

## Changed

- Pin direct `devDependencies` in `package.json` to exact versions already resolved by `package-lock.json`.
- Keep `peerDependencies.ioredis` as a compatibility range so consumers can provide their own Redis client version.
- Sync `package-lock.json` root metadata with `cache-hub@2.2.2`.

## Compatibility

- No runtime API changes.
- Node.js baseline remains `>=18.0.0`.
